{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Task management application with React frontend and Node.js backend",
  "scripts": {
    "test": "playwright test",
    "test:auth": "playwright test auth.spec.ts",
    "test:tasks": "playwright test tasks.spec.ts",
    "test:admin": "playwright test admin.spec.ts",
    "test:persistence": "playwright test persistence.spec.ts",
    "test:report": "playwright show-report",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "better-sqlite3": "^12.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0"
  }
}
